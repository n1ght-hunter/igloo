[project]
name = "python-plugin"
version = "0.1.0"
description = "Example Python plugin for Igloo - Task Manager application"
requires-python = ">=3.11"
dependencies = [
    "igloo-py",
]

[dependency-groups]
dev = [
    "componentize-py>=0.16",
]

[tool.uv.sources]
igloo-py = { workspace = true }
